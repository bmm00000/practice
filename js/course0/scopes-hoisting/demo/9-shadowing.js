const myAge = 26;

function printAge() {
	const myAge = 36;
	console.log(myAge);
}

printAge();

// variable shadowing: you can re-declare an already existing variable if you are doing it in a lower scope.
